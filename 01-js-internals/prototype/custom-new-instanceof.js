// custom-new-instanceof.js
// Implement `new` and `instanceof` from scratch to understand them.
//
// TODO:
//  - myNew(Ctor, ...args): create obj, link prototype, run Ctor, handle return
//  - myInstanceof(obj, Ctor): walk the prototype chain checking Ctor.prototype
