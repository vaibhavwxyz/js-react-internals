// forward-ref-portal.jsx
// forwardRef + createPortal — a Modal rendered outside the DOM hierarchy.
//
// TODO:
//  - forwardRef to expose the dialog node to parents
//  - ReactDOM.createPortal into document.body
//  - trap focus / close on Escape (optional)
