// compound-tabs — the compound component pattern.
// Tabs + TabList + Tab + TabPanel sharing state via context.
//
// TODO:
//  - Tabs: holds active index in context
//  - TabList/Tab: render triggers, set active on click
//  - TabPanel: render only when its index is active
//  - export all parts (e.g. Tabs.List, Tabs.Tab, Tabs.Panel)
