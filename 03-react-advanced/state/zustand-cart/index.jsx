// zustand-cart — AFTER.
// The same cart with Zustand + selectors for fine-grained subscriptions.
//
// TODO:
//  - create() store with actions
//  - components subscribe via selectors so only relevant ones re-render
//  - compare re-render count against the context-reducer version
