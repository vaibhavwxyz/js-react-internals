// use-event-listener.js
// useEventListener(event, handler, target): add/remove safely.
//
// TODO:
//  - keep latest handler in a ref to avoid re-subscribing each render
//  - default target to window; clean up on unmount
